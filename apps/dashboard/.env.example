# Base URL of the FiberMeter API (Express backend).
# The dashboard talks to this in "Live" mode. In "Demo" mode it runs a fully
# in-browser billing engine and never makes network calls.
VITE_API_URL=http://localhost:4000/api
VITE_DEMO_SERVICE_URL=http://localhost:5174
