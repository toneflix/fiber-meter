import crypto from 'node:crypto';
export type FiberMeterOptions={apiKey?:string;token?:string;baseUrl:string};
export class FiberMeter{constructor(private options:FiberMeterOptions){} private async request(path:string,init:RequestInit={}){const headers={ 'content-type':'application/json', ...(this.options.apiKey?{'x-api-key':this.options.apiKey}:{}), ...(this.options.token?{authorization:`Bearer ${this.options.token}`}:{}), ...(init.headers as object)}; const res=await fetch(`${this.options.baseUrl.replace(/\/$/,'')}/api${path}`,{...init,headers}); if(!res.ok)throw new Error(`FiberMeter request failed: ${res.status} ${await res.text()}`); return res.json()} recordUsage(input:{service:string;customer:string;metricKey:string;quantity:number;idempotencyKey:string;metadata?:Record<string,unknown>}){return this.request('/usage-events',{method:'POST',body:JSON.stringify(input)})} createCustomer(input:{externalId:string;name?:string;email?:string;metadata?:Record<string,unknown>}){return this.request('/customers',{method:'POST',body:JSON.stringify(input)})} createPaymentRequest(input:{customerId:string;amount:string;asset:string;metadata?:Record<string,unknown>}){return this.request('/payment-requests',{method:'POST',body:JSON.stringify(input)})} getBalance(customerId:string,asset='CKB'){return this.request(`/customers/${customerId}/balances/${asset}`)}}
export function verifyWebhookSignature(payload:string,signature:string,secret:string,timestamp:string){const expected=crypto.createHmac('sha256',secret).update(`${timestamp}.${payload}`).digest('hex'); return crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(signature))}
